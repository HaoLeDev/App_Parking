@model App_Parking.Models.LoginModel
@{
    ViewBag.Title = "Login1";
    Layout = "~/Views/Shared/_Layout_Login.cshtml";
}
@*<script>
    $(document).ready(function () {
        $("#btnLogin").click(function () {
            if ($("#UserName").val() == "") {
                alertmsg.error("Bạn chưa nhập tên đăng nhập!");
                //alertmsg.success("Đăng nhập thành công");
                $("#UserName").focus();
                return false;     
            }
            if ($("#Password").val() == "") {
                alertmsg.error("Bạn chưa nhập mật khẩu!");
                $("#Password").focus();
                return false;
            }
            alertmsg.success("Đăng nhập thành công");
            //$.ajax({
            //    type: 'post',
            //    url: 'Login',
            //    data: JSON.stringify({ Accout: _account }),
            //    dataType: 'json',
            //    contentType: 'application/json',
            //    success: function (res) {
            //        alertmsg.success("Đăng nhập thành công");
            //    },
            //    error: function () {
            //        alertmsg.error("Tên tài khoản hoặc mật khẩu không chính xác!");
            //    }
            //})
        })
        
    });
</script>*@
@using (Html.BeginForm("Login", "Login"))

{
<div class="login-box">
    <div class="login-logo">
        <a href="#"><b>Đăng nhập</b></a>
    </div>
    <!-- /.login-logo -->
    <div class="login-box-body">
        <p class="login-box-msg">Đăng nhập hệ thống</p>

        <form action="#" method="post" id="loginForm">
            @Html.ValidationSummary(false, "", new { @class = "text-danger" })
            <div class="form-group has-feedback">
                @Html.TextBoxFor(m => m.UserName, new { @id = "UserName", @name = "UserName", @class = "form-control", @placeholder = "Tên đăng nhập" })
                <span class="glyphicon glyphicon-user form-control-feedback"></span>
            </div>
            <div class="form-group has-feedback">
                @Html.PasswordFor(m => m.Password, new { @id = "Password", @name = "Password", @class = "form-control", @placeholder = "Mật khẩu" })
                <span class="glyphicon glyphicon-lock form-control-feedback"></span>
            </div>
            <div class="row">
                <div class="col-xs-8">
                    <div class="checkbox icheck">
                        <label>
                            @Html.CheckBoxFor(model => model.RememberMe) Nhớ mật khẩu
                        </label>
                    </div>
                </div>
                <!-- /.col -->
                <div class="col-xs-4">
                    <button type="submit" id="btnLogin" onclick="Js_Login.Login();" class="btn btn-primary btn-block btn-flat">Đăng nhập</button>
                </div>
                <!-- /.col -->
            </div>
        </form>

        <div class="social-auth-links text-center">
            <p>- Hoặc -</p>
            <a href="#" class="btn btn-block btn-social btn-facebook btn-flat">
                <i class="fa fa-facebook"></i> Đăng nhập với
                Facebook
            </a>
            <a href="#" class="btn btn-block btn-social btn-google btn-flat">
                <i class="fa fa-google-plus"></i> Đăng nhập với
                Google+
            </a>
        </div>
        <!-- /.social-auth-links -->

        <a href="#">Quên mật khẩu</a><br>
        <a href="register.html" class="text-center">Đăng ký</a>

    </div>
    <!-- /.login-box-body -->
</div>
}
<script src="~/Scripts/Js-Business/Login.js"></script>
